import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: '100%',
    flex: 1,
    paddingHorizontal: 15
  },
  title: {
    fontSize: 28,
    paddingVertical: 10,
    textAlign: "center",
    fontWeight: 'bold',
  },
});