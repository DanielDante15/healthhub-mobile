import { View, Text } from 'react-native';
import { styles } from './styles'



export default function Dietas({ }) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}> Tela de Dieta</Text>
    </View>
  );
}
